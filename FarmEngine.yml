database:
  driverClass: com.mysql.jdbc.Driver
  user: root
  password: mysql
  url: jdbc:mysql://localhost:3306/cropdb
  properties:
    charSet: UTF-8
  maxWaitForConnection: 1s

  validationQuery: "select * from firstable;"
  minSize: 8
  maxSize: 32
  checkConnectionWhileIdle: false
