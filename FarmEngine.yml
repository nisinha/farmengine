database:
  driverClass: org.postgresql.Driver
  user: postgres
  password: 
  url: jdbc:postgresql://localhost:5432/cropdb
  properties:
    charSet: UTF-8
  maxWaitForConnection: 1s

  validationQuery: "select * from farmlocation;"
  minSize: 8
  maxSize: 32
  checkConnectionWhileIdle: false
